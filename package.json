{
  "name": "wel-influx-logger",
  "description": "An InfluxDB logging server for the welserver.com one-wire monitor.",
  "license": "ISC",
  "version": "0.1.0",
  "engines": {
    "node": "^20.0.0"
  },
  "scripts": {
    "compile": "./node_modules/.bin/tsc",
    "compile-watch": "./node_modules/.bin/tsc -W",
    "lint": "./node_modules/.bin/tslint -s tslint.json -t verbose --project .",
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.18.2",
    "influx": "*",
    "typescript": "^4.9.5",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/express": "*",
    "@types/node": "*",
    "tslint": "*",
    "tslint-eslint-rules": "*"
  },
  "packageManager": "yarn@3.8.7"
}
